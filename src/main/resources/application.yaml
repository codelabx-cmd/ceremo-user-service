spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/ceremo_user
    username: root
    password: Ashishmac@2026
    jpa:
      hibernate:
        ddl-auto: update   # auto create/update tables
      show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080